---
name: Build and Deploy webapp

env:
  #Artifact Name: csye6225-webapp-${{github.run_number}}.zip
  ARTIFACT_NAME: csye6225-webapp-${{github.run_number}}.zip
  CODEDEPLOY_APPLICATION_NAME: mywebapp
  CODEDEPLOY_APPLICATION_DEPLOYMENT_GROUP_NAME: mywebapp
  AWS_REGION: us-west-2
  DB_HOSTNAME: localhost
  DB_PORT: 3306
  DB_DATABASE: csye6225
  DB_USER: root
  DB_PASSWORD: 'root'
  aws_secret_access_key: 3WuR0Zshd29kM7fc5JEpL7KQX7SooXVlF7OySMvV
  aws_access_key_id: AKIARSRCF6UBSLKMORCF
  aws_bucket_name: qqazc.dev.domain.tld20211031013007559900000001

on:
  pull_request:
      branches: [ main ]

jobs:
  ci_cd:
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v2
      - name: Setup JDK 1.8
        uses: actions/setup-java@v1
        with: 
          java-version: 1.8
      - name: Set up MySQL
        run: |
          sudo /etc/init.d/mysql start
          sudo mysql -e 'CREATE DATABASE ${{ env.DB_DATABASE }};' -u${{ env.DB_USER }} -p${{ env.DB_PASSWORD }}
          sudo mysql -e 'SHOW DATABASES;' -uroot -proot
      - name: Build with maven
        run: |
          echo "***************************************Printitng environment variables***************************************************"
          cat /etc/environment
          echo "******************************************************************************************"
          echo "********************************************Starting mvn clean install**********************************************"
          mvn clean install
          pwd
          echo "********************************************Build with maven List ls**********************************************"
          ls
          # pwd
          # ls
      - name: Build Deployemnt Artifact
        run: |
          mkdir codedeploy_artifact
          zip -r ${{env.ARTIFACT_NAME}} codedeploy_artifact/
          echo "********************************************Build Deployemnt Artifact List ls**********************************************"
          ls
          pwd
          cd codedeploy_artifact
          pwd
          echo "********************************************codedeploy_artifact List ls**********************************************"
          ls
          cd .. 
          pwd
          ls
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with: 
          aws-access-key-id: ${{secrets.AWS_ACCESS_KEY}}
          aws-secret-access-key: ${{secrets.AWS_SECRET_KEY}}
          aws-region: ${{secrets.AWS_REGION}}
